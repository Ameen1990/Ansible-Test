---
  - name: bring_nano
    hosts: my_group
    tasks:
      - name: ensure nano editor is there
        yum:
          name: nano
          state: latest
        when : ansible_distribution == "CentOS"
 
# Jetzt installieren wir mehrere Packages mit einzigen Job
# Und auch nur für CentOS Betrieb System

  - name: Sonstige packages Herunerladen (apache und PHP) 
    dnf: 
      name:
       - httpd
       - php
      stat: latest
    when : ansible_distribution == "CentOS"
   
